/** Default label for the built-in push-to-talk modifier (uiohook `AltRight`).
 *  The renderer prefers `window.electron.pushToTalkKeyLabel` (Right Option on macOS, Right Alt on Windows). */
export const PUSH_TO_TALK_KEY_LABEL = 'Right Option';
