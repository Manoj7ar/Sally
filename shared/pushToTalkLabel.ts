/** Label shown in UI for the global push-to-talk modifier (uiohook AltRight).
 *  Sally is macOS only, so the modifier is always Right Option. */
export const PUSH_TO_TALK_KEY_LABEL = 'Right Option';
