# Sally environment variables (optional — keys can also be set in the app UI)

# Gemini API key (vision, planning, and default speech-to-text)
GEMINI_API_KEY=AIza...

# Text-to-Speech (ElevenLabs)
ELEVENLABS_API_KEY=...

# Speech-to-text uses the same Gemini key as above; no separate STT key.
